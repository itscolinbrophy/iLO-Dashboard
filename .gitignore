# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# iLO endpoint credentials (never commit)
server/endpoints.json
# Homelab service config contains live credentials (never commit)
server/homelab-config.json
# Telemetry history cache (never commit)
server/history/
# SSH keys for passwordless terminal access (never commit)
server/ssh-keys/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
